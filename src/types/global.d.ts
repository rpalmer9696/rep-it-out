export type Exercise = {
  id: string;
  name: string;
  weight: number;
  reps: number;
};

export type Food = {
  id: string;
  name: string;
  amount: number;
  date: Date;
};

export type Supplement = {
  id: string;
  name: string;
  amount: number;
  date: Date;
};
